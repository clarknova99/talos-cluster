---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mfsoft
  namespace: default
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 30m

  values:
    controllers:
      mfsoft:
        replicas: 1
        strategy: RollingUpdate

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/clarknova99/mfsoft-web
              tag: main-601422bf1f8857bf454b475deddd894885e61f55@sha256:1e0a9bae65cfbaa01642e63f79b6184993332c51889ff19eacc1e16317b80ad1
            resources:
              requests:
                cpu: 5m
                memory: 128Mi
              limits:
                memory: 256Mi
        pod:
          # imagePullPolicy: Always
          imagePullSecrets:
            - name: mfsoft-secret
    service:
      app:
        ports:
          http:
            port: 8000
    route:
      main:
        annotations:
          external-dns.alpha.kubernetes.io/target: external.${SECRET_DOMAIN_THREE}
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Infra
          gethomepage.dev/icon: kubernetes-dashboard.svg
          gatus.home-operations.com/endpoint: |
            group: observability
        parentRefs:
          - name: envoy-external
            namespace: network
            sectionName: https
        hostnames:
          - ${SECRET_DOMAIN_THREE}
          - www.${SECRET_DOMAIN_THREE}
    persistence:
      data:
        enabled: true
        type: nfs
        server: 192.168.1.3
        path: /volume1/network-storage/cluster/mfsoft
        globalMounts:
          - path: /app/data
