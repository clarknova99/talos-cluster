---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dittofeed-temporal
  namespace: sensei
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 30m

  values:
    controllers:
      dittofeed-temporal:
        containers:
          temporal:
            image:
              repository: temporalio/auto-setup
              tag: 1.29.6
            env:
              DB: postgres12
              DB_PORT: 5432
              POSTGRES_USER: ${SECRET_PGADMIN_USER}
              POSTGRES_PWD: ${SECRET_PGADMIN_PASSWORD}
              POSTGRES_SEEDS: postgres-lb-vector.database.svc.cluster.local
          temporal-ui:
            image:
              repository: temporalio/ui
              tag: 2.49.1
            env:
              DB: postgres12
              DB_PORT: 5432
              POSTGRES_USER: ${SECRET_PGADMIN_USER}
              POSTGRES_PWD: ${SECRET_PGADMIN_PASSWORD}
              POSTGRES_SEEDS: postgres-lb-vector.database.svc.cluster.local
              TEMPORAL_ADDRESS: dittofeed-temporal.sensei.svc.cluster.local:7233
    service:
      temporal:
        controller: dittofeed-temporal
        ports:
          grpc:
            port: 7233
      temporal-ui:
        controller: dittofeed-temporal
        type: LoadBalancer
        annotations:
          io.cilium/lb-ipam-ips: 192.168.3.37
        ports:
          http:
            port: &port 8080
    route:
      main:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: "Sensei Services"
          gethomepage.dev/icon: https://docs.temporal.io/img/favicon.png
          gatus.home-operations.com/endpoint: |
            group: sensei
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        hostnames:
          - dittofeed-temporal.${SECRET_DOMAIN_TWO}
        rules:
          - backendRefs:
              - name: dittofeed-temporal-temporal-ui
                port: *port
