---
apiVersion: v1
kind: Secret
metadata:
  name: dittofeed-clickhouse-volsync-secret
stringData:
  KOPIA_PASSWORD: ${VOLSYNC_KOPIA_PASSWORD}
  KOPIA_FS_PATH: /repository
  KOPIA_REPOSITORY: "filesystem:///repository"
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: dittofeed-clickhouse
spec:
  sourcePVC: dittofeed-clickhouse
  trigger:
    schedule: "0 2 * * *"
  kopia:
    compression: zstd-fastest
    copyMethod: Direct
    parallelism: 2
    repository: dittofeed-clickhouse-volsync-secret
    retain:
      daily: 7
    storageClassName: openebs-hostpath
    moverSecurityContext:
      runAsUser: 101
      runAsGroup: 101
      fsGroup: 101
    accessModes: ["ReadWriteOnce"]
