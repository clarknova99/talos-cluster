---
# Placeholder — openreplay-minio ExternalName service was removed.
# MinIO is now referenced directly from the HTTPRoute via a cross-namespace
# backendRef (kubernetes/apps/kube-system/minio/app/referencegrant.yaml).
# This file is kept so kustomization.yaml does not need to be edited.
apiVersion: v1
kind: ConfigMap
metadata:
  name: openreplay-minio-placeholder
  namespace: sensei
data: {}
