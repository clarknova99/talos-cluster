---
# Kopia password is injected via Flux postBuild substituteFrom (cluster-secrets)
apiVersion: v1
kind: Secret
metadata:
  name: kopia-secret
stringData:
  KOPIA_PASSWORD: ${VOLSYNC_KOPIA_PASSWORD}
