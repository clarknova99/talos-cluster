---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mfsoft-web
  namespace: mfsoft
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 30m

  values:
    controllers:
      mfsoft-web:
        replicas: 1
        strategy: RollingUpdate

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/mfsoft-dev/mfsoft-web
              tag: main-098f03afdee28c4ec96243c8047f3bbcb0c0e0d4@sha256:be7e493a921850bd2dcd78dfcb5b28f4cd713310230bc2393f9c906b9656521b
            env:
              POLAR_PRO_CHECKOUT_URL: ${SECRET_MFSOFT_POLAR_PRO_CHECKOUT_URL}
              POLAR_INFRA_CHECKOUT_URL: ${SECRET_MFSOFT_POLAR_INFRA_CHECKOUT_URL}
            resources:
              requests:
                cpu: 5m
                memory: 128Mi
              limits:
                memory: 256Mi
        pod:
          # imagePullPolicy: Always
          imagePullSecrets:
            - name: mfsoft-secret
    service:
      app:
        ports:
          http:
            port: 8000
    route:
      main:
        annotations:
          external-dns.alpha.kubernetes.io/target: external.${SECRET_DOMAIN_THREE}
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: MFSoft
          gethomepage.dev/icon: kubernetes-dashboard.svg
          gatus.home-operations.com/endpoint: |
            group: mfsoft
        parentRefs:
          - name: envoy-external
            namespace: network
            sectionName: https
        hostnames:
          - ${SECRET_DOMAIN_THREE}
          - www.${SECRET_DOMAIN_THREE}
    persistence:
      data:
        enabled: true
        type: nfs
        server: 192.168.1.3
        path: /volume1/network-storage/cluster/mfsoft
        globalMounts:
          - path: /app/data
