---
apiVersion: v1
kind: Secret
metadata:
  name: mfsoft-dittofeed-clickhouse-volsync-secret
stringData:
  KOPIA_PASSWORD: ${VOLSYNC_KOPIA_PASSWORD}
  KOPIA_FS_PATH: /repository
  KOPIA_REPOSITORY: "filesystem:///repository"
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: mfsoft-dittofeed-clickhouse
spec:
  sourcePVC: mfsoft-dittofeed-clickhouse
  trigger:
    schedule: "0 3 * * *"
  kopia:
    compression: zstd-fastest
    copyMethod: Direct
    parallelism: 2
    repository: mfsoft-dittofeed-clickhouse-volsync-secret
    retain:
      daily: 7
    storageClassName: openebs-hostpath
    moverSecurityContext:
      runAsUser: 101
      runAsGroup: 101
      fsGroup: 101
    accessModes: ["ReadWriteOnce"]
