---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mfsoft-dittofeed-temporal
  namespace: mfsoft
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 30m

  values:
    controllers:
      mfsoft-dittofeed-temporal:
        containers:
          temporal:
            image:
              repository: temporalio/auto-setup
              tag: 1.29.7
            env:
              DB: postgres12
              DB_PORT: 5432
              POSTGRES_USER: ${SECRET_PGADMIN_USER}
              POSTGRES_PWD: ${SECRET_PGADMIN_PASSWORD}
              POSTGRES_SEEDS: postgres16-rw.database.svc.cluster.local
          temporal-ui:
            image:
              repository: temporalio/ui
              tag: 2.51.0
            env:
              DB: postgres12
              DB_PORT: 5432
              POSTGRES_USER: ${SECRET_PGADMIN_USER}
              POSTGRES_PWD: ${SECRET_PGADMIN_PASSWORD}
              POSTGRES_SEEDS: postgres16-rw.database.svc.cluster.local
              TEMPORAL_ADDRESS: mfsoft-dittofeed-temporal.mfsoft.svc.cluster.local:7233
    service:
      temporal:
        controller: mfsoft-dittofeed-temporal
        ports:
          grpc:
            port: 7233
      temporal-ui:
        controller: mfsoft-dittofeed-temporal
        type: LoadBalancer
        annotations:
          io.cilium/lb-ipam-ips: 192.168.3.41
        ports:
          http:
            port: &port 8080
    route:
      main:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: "MFSoft"
          gethomepage.dev/icon: https://docs.temporal.io/img/favicon.png
          gatus.home-operations.com/endpoint: |
            group: mfsoft
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        hostnames:
          - temporal.${SECRET_DOMAIN_THREE}
        rules:
          - backendRefs:
              - name: mfsoft-dittofeed-temporal-temporal-ui
                port: *port
